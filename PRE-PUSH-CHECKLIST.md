# Pre-Push Checklist

- Confirm `inc/config.php` has no real secrets.
- Confirm `data.sqlite` is not committed.
- Confirm `uploads/` contains no customer files.
- Confirm no `error_log` files remain.
- Run a PHP syntax check locally.
- Test homepage, login, admin, and API routes.
